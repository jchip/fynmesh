/** One of the combined bundle's members. Imported by `main`, so it executes. */
export function hello(): string {
  return "Hello from fynapp-bundled — this module arrived inside a combined bundle.";
}
