# fynapp-5-preact

FynApp using Preact (without React compat layer).

## Framework
Preact 10

## Features
- Native Preact (no React compatibility)
- Lightweight alternative to React
- Custom elements support via preact-custom-element

## Build
```bash
fyn install
nvx rollup -c
```
