# fynapp-1

React 19 FynApp demonstrating middleware consumption and design tokens.

## Framework
React 19

## Features
- Consumes `fynapp-react-middleware` for theme/user context
- Uses `fynapp-design-tokens` for theming
- Demonstrates middleware configuration patterns

## Build
```bash
fyn install
nvx rollup -c
```
