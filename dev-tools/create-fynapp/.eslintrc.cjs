module.exports = {
  root: true,
  env: {
    es2022: true,
    jest: true,
    node: true,
  },
  extends: ["eslint:recommended"],
  parser: "@typescript-eslint/parser",
  parserOptions: {
    ecmaVersion: "latest",
    sourceType: "module",
  },
  plugins: ["@typescript-eslint"],
  // test/demo holds apps generated by the CLI during tests - it is gitignored
  // output, not source, so linting it makes the result depend on whether the
  // tests have been run yet
  ignorePatterns: ["dist", "docs", "templates", "test/demo"],
  rules: {
    "no-undef": "off",
    "no-unused-vars": "off",
    "@typescript-eslint/no-unused-vars": [
      "error",
      { argsIgnorePattern: "^_", varsIgnorePattern: "^_" },
    ],
  },
};
